# Example environment variables for deployment (DO NOT commit real secrets)
# Set these in your Render Dashboard under "Environment" for the Web Service.

# Django secret key (generate a secure value and set here)
DJANGO_SECRET_KEY=changeme_generate_a_secure_key

# Django debug (should be False in production)
DEBUG=False

# Database URL (Render managed Postgres example)
# Example: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=sqlite:///db.sqlite3

# OpenAI API key (optional — only if using hints/features)
OPENAI_API_KEY=

# Allowed hosts (comma-separated) — Render will provide an external URL
ALLOWED_HOSTS=*

# Optional: other env vars
# EMAIL_HOST, EMAIL_PORT, etc.

# To generate a secure Django secret key locally, run:
# python - <<'PY'
# import secrets, string
# alphabet = string.ascii_letters + string.digits + string.punctuation
# print(''.join(secrets.choice(alphabet) for _ in range(50)))
# PY

# On Render: set DJANGO_SECRET_KEY to a securely-generated value and
# set DATABASE_URL to the managed Postgres (recommended) or leave as SQLite.
